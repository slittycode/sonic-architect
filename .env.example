# Gemini API key — required only if using the Gemini provider
# Get one at https://aistudio.google.com/
GEMINI_API_KEY=

# Anthropic API key — required only if using the Claude provider
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Vite exposes GEMINI_API_KEY via this alias (set automatically by vite.config.ts)
# You do not need to set this manually.
# VITE_GEMINI_API_KEY=
